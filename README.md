# 🚀 Портфолио Ярослава Бесфалина

Персональный сайт-портфолио с блогом на React + Vite.

---

## 📋 Что это?

- **Портфолио** — информация обо мне, проектах, интересах
- **Блог** — система блогов на основе Markdown файлов
- **Автоматизация** — всё работает автоматически, просто пиши посты!

---

## 🛠️ Технологии

- **React 18** — UI библиотека
- **Vite** — сборщик и dev-сервер
- **React Router** — маршрутизация
- **React Markdown** — рендеринг Markdown
- **GitHub Pages** — хостинг

---

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск локально

```bash
npm run dev
```

Откроется на `http://localhost:4000`

### Деплой на GitHub Pages

```bash
npm run deploy
```

---

## 📝 Работа с блогом

### Как добавить новый пост?

1. **Создай файл** в папке `blog/`:

   ```
   blog/мой-новый-пост.md
   ```

2. **Напиши пост** в Markdown:

   ```markdown
   # Заголовок поста

   Текст поста с **форматированием**.
   ```

3. **Добавь изображение** (если нужно):

   - Помести файл в `blog/` (например: `blog/photo.png`)
   - В посте используй: `![Описание](/blog/photo.png)`

4. **Задеплой**:
   ```bash
   npm run deploy
   ```

**Всё!** Пост появится на сайте через минуту.

---

## 📁 Структура проекта

```
danteBes.github.io/
├── blog/                    # 📝 Твоя рабочая папка для постов
│   ├── мой-пост.md         # Посты в формате Markdown
│   └── image.png           # Изображения для постов
│
├── public/blog/             # 🤖 Автоматически генерируется
│   ├── blog-list.json      # Список всех постов
│   └── ...                 # Скопированные файлы из blog/
│
├── src/
│   ├── components/          # React компоненты
│   ├── pages/              # Страницы сайта
│   │   ├── main-page/      # Главная страница
│   │   ├── blog-list/      # Список блогов
│   │   └── blog-post/      # Страница поста
│   └── utils/              # Утилиты
│
└── scripts/
    ├── sync-blog-files.js      # Копирует файлы из blog/ в public/blog/
    └── generate-blog-list.js    # Генерирует список постов
```

---

## ⚙️ Команды

| Команда                      | Что делает                                               |
| ---------------------------- | -------------------------------------------------------- |
| `npm run dev`                | Запускает dev-сервер (автоматически синхронизирует блог) |
| `npm run build`              | Собирает проект (автоматически синхронизирует блог)      |
| `npm run deploy`             | Деплоит на GitHub Pages                                  |
| `npm run sync-blog`          | Вручную синхронизирует файлы из `blog/` в `public/blog/` |
| `npm run generate-blog-list` | Генерирует список постов                                 |

---

## 🔄 Как это работает?

### Автоматизация блога

1. **Ты пишешь** посты в папке `blog/`
2. **При запуске** `dev` или `build`:
   - Файлы копируются из `blog/` → `public/blog/`
   - Генерируется `blog-list.json` с датами
3. **При деплое** всё автоматически публикуется

### Формат постов

- **Название файла**: `мой-пост.md` (с дефисами)
- **Название на сайте**: "Мой пост" (автоматически преобразуется)
- **Дата обновления**: определяется автоматически по дате файла

---

## 📍 Страницы сайта

- `/` — Главная страница (Обо мне, Проекты, Интересы, Блог)
- `/about` — Подробная информация обо мне
- `/blogs` — Список всех постов
- `/blogs/имя-поста` — Отдельный пост

---

## 🖼️ Изображения в постах

### Поддерживаемые форматы

- PNG, JPG, JPEG, GIF, WebP, SVG

### Как использовать

1. Помести изображение в `blog/`
2. В посте используй:
   ```markdown
   ![Описание](/blog/имя-файла.png)
   ```

**Важно:** Всегда используй абсолютные пути `/blog/...`

---

## 🎨 Стилизация

- CSS Modules для компонентов
- CSS переменные для темы (`variables.css`)
- Адаптивный дизайн
- Плавные переходы и анимации

---

## 📦 Деплой

### GitHub Pages

1. Убедись, что репозиторий настроен для GitHub Pages
2. Запусти:
   ```bash
   npm run deploy
   ```
3. Через 1-2 минуты сайт обновится

### Что происходит при деплое?

1. Синхронизация файлов из `blog/` в `public/blog/`
2. Генерация списка постов
3. Сборка проекта в `dist/`
4. Публикация на GitHub Pages

---

## ❓ Частые вопросы

### Пост не появляется на сайте?

- Проверь, что файл в папке `blog/` (не в `public/blog/`)
- Убедись, что название файла без пробелов (используй дефисы)
- Запусти `npm run sync-blog` вручную

### Изображение не отображается?

- Проверь, что используешь абсолютный путь `/blog/...`
- Убедись, что файл скопировался в `public/blog/`

### Как изменить порядок постов?

- Посты сортируются по алфавиту названий
- Можно изменить порядок в `public/blog/blog-list.json` (но он перезапишется при следующем деплое)

---

## 📚 Дополнительно

- **Markdown**: Полная поддержка стандартного Markdown + GFM (таблицы, зачеркивание)
- **Даты**: Автоматически определяются по дате создания/редактирования файла
- **Навигация**: Якоря работают с любой страницы (автоматически ведут на главную)

---

## 🎯 Итого

**Просто пиши посты в `blog/` и запускай `npm run deploy`!** Всё остальное работает автоматически. 🚀

---

## 📞 Контакты

- **VK**: [vk.com/besfalin_yt](https://vk.com/besfalin_yt)
- **YouTube**: [@DanteBesfalin](https://youtube.com/@DanteBesfalin)
- **Telegram**: [@dantebesfalin](https://t.me/dantebesfalin)
