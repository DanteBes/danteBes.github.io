# Блог

Эта папка содержит все посты блога в формате Markdown. **Важно:** эта папка генерируется автоматически! Всегда работайте с папкой `blog/` в корне проекта.

## ⚠️ Важно

**НЕ редактируйте файлы напрямую в этой папке!** Все файлы здесь автоматически копируются из папки `blog/` в корне проекта при запуске `npm run dev` или `npm run build`.

---

## 📝 Как добавить новый пост

### Шаг 1: Создайте MD файл
1. Откройте папку `blog/` в **корне проекта** (не эту папку!)
2. Создайте новый файл с расширением `.md`
3. Название файла должно быть в формате slug (например: `мой-новый-пост.md`)

### Шаг 2: Напишите пост
Откройте файл и напишите текст в формате Markdown:

```markdown
# Заголовок поста

Текст вашего поста с **форматированием**.
```

### Шаг 3: Добавьте изображения (если нужно)
1. Поместите изображение (PNG, JPG, GIF, WebP, SVG) в папку `blog/`
2. В посте используйте:
```markdown
![Описание](/blog/имя-файла.png)
```

### Шаг 4: Проверьте локально
```bash
npm run dev
```
Откройте `http://localhost:4000/blogs` и проверьте, что пост появился.

---

## 🚀 Как задеплоить изменения на GitHub Pages

### Вариант 1: Быстрый деплой (всё автоматически)
```bash
npm run deploy
```
Эта команда автоматически:
- Скопирует файлы из `blog/` в `public/blog/`
- Сгенерирует список блогов с датами
- Соберёт проект
- Задеплоит на GitHub Pages

### Вариант 2: Пошагово (если нужно проверить каждый этап)

**Шаг 1:** Синхронизация блога
```bash
npm run sync-blog
```
Копирует MD файлы и изображения из `blog/` в `public/blog/`

**Шаг 2:** Генерация списка
```bash
npm run generate-blog-list
```
Создаёт/обновляет `blog-list.json` с датами создания и обновления

**Шаг 3:** Сборка проекта
```bash
npm run build
```
Создаёт папку `dist/` с готовым сайтом

**Шаг 4:** Деплой
```bash
npm run deploy
```
Отправляет `dist/` на GitHub Pages

---

## 📋 Формат названий файлов

- Используйте дефисы для разделения слов: `мой-первый-пост.md`
- Название на сайте будет автоматически преобразовано: "Мой первый пост"
- Не используйте пробелы или специальные символы в названиях файлов

---

## ✍️ Форматирование

Посты поддерживают стандартный Markdown:

- Заголовки (`# ## ###`)
- **Жирный текст** и _курсив_
- Списки (нумерованные и маркированные)
- Код (inline и блоки)
- Ссылки и изображения
- Цитаты
- Таблицы (через GFM)
- И многое другое!

### Пример поста

```markdown
# Пример поста

Это пример поста в блоге.

## Раздел

Текст с **форматированием** и _курсивом_.

### Подзаголовок

- Список элементов
- Еще один элемент

```javascript
// Пример кода
function hello() {
  console.log("Привет!");
}
```

![Изображение](/blog/image.png)
```

---

## 🖼️ Работа с изображениями

### Поддерживаемые форматы
- PNG (`.png`)
- JPEG (`.jpg`, `.jpeg`)
- GIF (`.gif`)
- WebP (`.webp`)
- SVG (`.svg`)

### Как использовать
1. Поместите изображение в папку `blog/` (в корне проекта)
2. В посте используйте абсолютный путь:
```markdown
![Описание изображения](/blog/имя-файла.png)
```

**Важно:** Всегда используйте абсолютные пути `/blog/...` для изображений в постах.

---

## 📍 Важные моменты

1. **Всегда работайте с папкой `blog/` в корне проекта**, не с `public/blog/`
2. После изменений запускайте `npm run deploy` — всё синхронизируется автоматически
3. Изменения появляются на сайте через 1-2 минуты после деплоя
4. URL поста: `ваш-сайт.github.io/blogs/имя-файла` (без `.md`)

---

## 🔄 Пример полного цикла

```bash
# 1. Создали новый пост blog/новый-пост.md
# 2. Добавили изображение blog/photo.png
# 3. Проверили локально
npm run dev

# 4. Задеплоили
npm run deploy

# Готово! Пост появится на сайте через минуту
```

---

## ❓ Если что-то не работает

1. Проверьте, что файл в папке `blog/` (в корне проекта), а не в `public/blog/`
2. Убедитесь, что название файла без пробелов (используйте дефисы)
3. Запустите `npm run sync-blog` вручную для проверки
4. Проверьте консоль на ошибки при деплое
5. Убедитесь, что изображения используют абсолютные пути `/blog/...`

---

## 📚 Дополнительная информация

- Даты создания и обновления постов определяются автоматически по дате файла
- Список постов генерируется автоматически при каждом деплое
- Все изменения синхронизируются автоматически при запуске `dev` или `build`

**Просто создавайте посты в `blog/` и запускайте `npm run deploy`!** 🎉